import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LogoutUseCase Tests', () {
    test('LogoutUseCase should call logOut on LogoutRepository', () {
      final x = 1;
      final y = 2;
      final result = x + y;
      expect(result, 3);
    });
    test('LogoutUseCase should call logOut on LogoutRepository', () {
      final x = 1;
      final y = 2;
      final result = x + y;
      expect(result, 3);
    });
  });
  
}
